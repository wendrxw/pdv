"""Janela nativa do shell (PyWebview).

FASE 1 — a implementar:
- criar a janela com tamanho mínimo e título do PDV;
- carregar `Config.server_url` + `/app/`;
- registrar a bridge `js_api` (whitelist: fechar/minimizar/maximizar,
  status de impressão, versão do shell, offline);
- bloquear F5/reload para não ressubmeter POSTs.

Este módulo é um stub intencional da Fase 0 (esqueleto).
"""
