from django.apps import AppConfig


class PrintingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.printing"
    verbose_name = "Impressão — Comprovantes e PrintJobs"
