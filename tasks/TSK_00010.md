# Analise completa

1. **Leitura compulsória:** `agents.md`.
2. **Verificação do tenant:** Antes de qualquer `Model.objects.get()`, confirmar se existe filtro por `tenant`.
3. **Django Admin:** Todo novo model deve ser registrado no admin com list_display e filters.
4. **Commit:** Usar Conventional Commits.


- Quero que você atue como um desenvolvedor python senior (django fullstack);
- Analise o projeto, verifique se esta tudo organizado para lançarmos o MVP;
- Verifique os arquivos de documentação e as tasks anteriores para obter conhecimento do projeto;
- Quaisquer alteração, ajuste, implementação necessária, crie um arquivo chamado MVP.md, dentro de tasks/ para implementação futura;
- Atue também como o cybersecurity agent senior, faça varreduras no codigo, procure vulnerabilidades, veja se tem algo que pode ser melhorado, atualize bibliotecas, mas faça tudo isso sem quebrar o projeto;
  

## Deploy em produção - Preparação

- Nesse caso, você irá preparar o terreno para deploy em produção com automatizações via script;

Você é o agente responsável por colocar este projeto Django em **produção**, em um servidor **Debian Linux**, utilizando:

* **Nginx** como reverse proxy
* **Gunicorn** para executar o Django
* **Cloudflare Tunnel (`cloudflared`)** para exposição externa
* Domínio de produção: **`pdv.wendrxw.online`**
* Aplicação: **Django**
* Banco de dados: utilizar o banco já configurado no projeto; não substituir por outro sem necessidade
* Sistema de arquivos/arquivos estáticos e mídia devem ser configurados corretamente para produção

## OBJETIVO

Colocar o sistema PDV em produção de forma segura, reproduzível e compatível com a infraestrutura existente.

O acesso final deverá funcionar em:

`https://pdv.wendrxw.online`

A arquitetura esperada é:

```text
Internet
   │
   ▼
Cloudflare
   │
   ▼
cloudflared tunnel
   │
   ▼
Nginx
   │
   ▼
Gunicorn
   │
   ▼
Django
   │
   ▼
Banco de dados
```

---

# REGRA MAIS IMPORTANTE

**NÃO comece modificando arquivos ou serviços.**

Primeiro faça uma auditoria completa do projeto e do servidor.

Você deve descobrir:

1. Estrutura do projeto Django.
2. Arquivo correto de `settings`.
3. Arquivo correto de `manage.py`.
4. Como o projeto atualmente inicia.
5. Dependências Python.
6. Versão do Python necessária.
7. Banco de dados utilizado.
8. Diretório de `static`.
9. Diretório de `media`, se existir.
10. Variáveis de ambiente necessárias.
11. Configurações de segurança já existentes.
12. Se existem migrations pendentes.
13. Se existem testes.
14. Como o projeto deve ser executado em produção.
15. Quais portas já estão sendo utilizadas no servidor.
16. Quais serviços systemd já existem.
17. Qual configuração atual do Nginx existe.
18. Qual configuração atual do Cloudflare Tunnel existe.

**Não faça suposições quando puder verificar.**

---

# IMPORTANTE: SERVIDOR COMPARTILHADO

Este servidor pode hospedar outros projetos.

Portanto:

**NÃO altere, remova, reinicie ou sobrescreva serviços existentes sem necessidade.**

Antes de modificar:

```bash
systemctl list-units --type=service --state=running
```

Verifique também:

```bash
ss -lntp
```

e:

```bash
ps aux
```

Identifique claramente quais portas e serviços já estão sendo utilizados.

O PDV deverá possuir seu próprio serviço/systemd e sua própria porta local.

Exemplo:

```text
127.0.0.1:PORTA_PDV
```

Escolha uma porta livre após verificar o servidor.

---

# ETAPA 1 — AUDITORIA DO PROJETO

Analise o repositório inteiro antes de fazer alterações.

Identifique:

```text
manage.py
pyproject.toml
requirements.txt
Pipfile
uv.lock
poetry.lock
settings.py
settings/
urls.py
wsgi.py
asgi.py
.env.example
Dockerfile
```

Não presuma que todos existem.

Determine qual gerenciador de dependências o projeto utiliza atualmente.

Se houver `pyproject.toml`, analise-o cuidadosamente.

Determine também a versão mínima e recomendada do Python.

---

# ETAPA 2 — AMBIENTE PYTHON

Não instale uma versão arbitrária do Python.

Primeiro descubra:

```bash
python3 --version
```

e a versão exigida pelo projeto.

Caso seja necessário instalar uma versão adicional do Python, faça isso sem quebrar o Python utilizado pelo sistema operacional.

Crie um ambiente virtual dedicado ao PDV.

Preferencialmente:

```text
/opt/pdv/
```

ou outro diretório apropriado para aplicações de produção.

Exemplo:

```text
/opt/pdv/
├── app/
├── venv/
├── logs/
└── ...
```

Não execute o Django em produção utilizando o usuário root.

---

# ETAPA 3 — CONFIGURAÇÃO DE PRODUÇÃO

Configure corretamente:

```python
DEBUG = False
```

Configure:

```python
ALLOWED_HOSTS
```

para incluir:

```text
pdv.wendrxw.online
```

Configure corretamente:

```python
CSRF_TRUSTED_ORIGINS
```

incluindo:

```text
https://pdv.wendrxw.online
```

Analise também:

```python
SESSION_COOKIE_SECURE
CSRF_COOKIE_SECURE
SECURE_PROXY_SSL_HEADER
SECURE_SSL_REDIRECT
SESSION_COOKIE_DOMAIN
CSRF_COOKIE_DOMAIN
```

Não copie configurações de outro projeto cegamente.

Como existe Cloudflare Tunnel, confirme como o HTTPS é terminado e garanta que o Django reconheça corretamente as requisições HTTPS.

O objetivo é evitar problemas como:

```text
CSRF verification failed
Invalid HTTP_HOST header
redirect loop
cookies não persistindo
request.is_secure() = False
```

---

# ETAPA 4 — SECRET KEY E CREDENCIAIS

A `SECRET_KEY` de produção **não pode ficar hardcoded no código**.

Utilize variáveis de ambiente.

Nunca exponha:

* SECRET_KEY
* senhas do banco
* tokens
* API keys
* credenciais do Cloudflare
* credenciais de serviços externos

no Git.

Verifique se `.env` está no `.gitignore`.

Se existir `.env.example`, atualize-o apenas com nomes das variáveis, nunca com segredos reais.

---

# ETAPA 5 — BANCO DE DADOS

Descubra qual banco o projeto utiliza.

Verifique as configurações Django:

```python
DATABASES
```

Execute:

```bash
python manage.py check --deploy
```

Depois:

```bash
python manage.py makemigrations --check
```

Se houver migrations pendentes, analise-as antes de executar.

Depois execute as migrations necessárias:

```bash
python manage.py migrate
```

**Não apague banco de dados, tabelas ou dados existentes.**

---

# ETAPA 6 — STATIC E MEDIA

Configure corretamente:

```bash
python manage.py collectstatic --noinput
```

Determine o `STATIC_ROOT`.

Se o projeto possuir upload de arquivos, determine também:

```python
MEDIA_ROOT
MEDIA_URL
```

Configure o Nginx para servir arquivos estáticos e mídia quando apropriado.

Não utilize `runserver` para produção.

---

# ETAPA 7 — GUNICORN

Configure Gunicorn para executar o Django.

Crie um serviço systemd dedicado, por exemplo:

```text
pdv.service
```

O serviço deve:

* iniciar automaticamente com o sistema;
* reiniciar caso o processo falhe;
* executar com usuário sem privilégios;
* utilizar o virtualenv correto;
* utilizar o diretório correto do projeto;
* carregar as variáveis de ambiente necessárias;
* registrar logs adequadamente.

Exemplo conceitual:

```text
systemd
   │
   └── pdv.service
          │
          └── gunicorn
                 │
                 └── Django
```

Use bind local:

```text
127.0.0.1:PORTA_PDV
```

Não exponha Gunicorn diretamente à Internet.

Determine uma quantidade adequada de workers considerando os recursos disponíveis no servidor.

Não simplesmente use uma fórmula sem analisar a máquina.

---

# ETAPA 8 — NGINX

Crie uma configuração específica para:

```text
pdv.wendrxw.online
```

Não sobrescreva configurações de outros sites.

O Nginx deverá fazer proxy para o Gunicorn.

Estrutura esperada:

```text
pdv.wendrxw.online
        │
        ▼
      Nginx
        │
        ▼
127.0.0.1:PORTA_PDV
        │
        ▼
    Gunicorn
```

Configure corretamente:

* `server_name`
* `proxy_pass`
* headers de proxy
* `X-Forwarded-Proto`
* `X-Forwarded-For`
* `Host`
* static files
* media files, se necessário
* timeouts adequados

Antes de recarregar:

```bash
nginx -t
```

Somente se estiver tudo correto:

```bash
systemctl reload nginx
```

---

# ETAPA 9 — CLOUDFLARED

O servidor já utiliza Cloudflare Tunnel.

**Não crie um novo tunnel sem antes verificar se já existe um tunnel funcional.**

Verifique:

```bash
cloudflared tunnel list
```

e:

```bash
systemctl status cloudflared
```

Analise a configuração existente.

Se já houver um tunnel utilizado por outros serviços, adicione o novo hostname sem quebrar os existentes.

O objetivo é adicionar:

```text
pdv.wendrxw.online
```

apontando para o Nginx local.

A arquitetura deverá ficar:

```text
pdv.wendrxw.online
        │
        ▼
Cloudflare
        │
        ▼
Cloudflare Tunnel
        │
        ▼
localhost:80
        │
        ▼
Nginx
        │
        ▼
Gunicorn
        │
        ▼
Django
```

**Não altere hostnames existentes do tunnel sem necessidade.**

Depois da alteração, valide que os serviços antigos continuam funcionando.

---

# ETAPA 10 — DNS

Verifique se:

```text
pdv.wendrxw.online
```

está corretamente associado ao Cloudflare Tunnel.

Não altere DNS de outros subdomínios.

Caso a configuração do Cloudflare Tunnel faça o gerenciamento necessário automaticamente, utilize o método já empregado pelo servidor.

---

# ETAPA 11 — SEGURANÇA

Execute:

```bash
python manage.py check --deploy
```

Analise todos os warnings.

Corrija os problemas relevantes para produção.

Verifique:

```text
DEBUG
ALLOWED_HOSTS
CSRF
cookies seguros
SECRET_KEY
HTTPS
HSTS
X-Frame-Options
Content-Type sniffing
referrer policy
```

Não habilite uma política de segurança agressiva que possa quebrar o sistema sem antes verificar a compatibilidade.

---

# ETAPA 12 — LOGS

Configure uma maneira clara de diagnosticar problemas.

Você deve conseguir verificar:

```bash
journalctl -u pdv.service
```

e:

```bash
journalctl -u pdv.service -f
```

Também valide:

```bash
journalctl -u nginx
```

e:

```bash
journalctl -u cloudflared
```

---

# ETAPA 13 — TESTES

Antes de considerar o deploy concluído, execute:

```bash
python manage.py check
```

```bash
python manage.py check --deploy
```

```bash
python manage.py test
```

Se o projeto possuir uma suíte específica de testes, execute-a também.

Teste localmente:

```bash
curl -I http://127.0.0.1
```

ou diretamente contra a porta do Gunicorn, quando apropriado.

Depois teste:

```text
https://pdv.wendrxw.online
```

Verifique:

* página inicial;
* login;
* sessão;
* CSRF;
* carregamento de CSS;
* carregamento de JavaScript;
* imagens;
* APIs;
* banco de dados;
* criação de registros;
* funcionalidades principais do PDV.

---

# ETAPA 14 — VERIFICAÇÃO DO PDV

Como este é um sistema de ponto de venda, dê atenção especial ao fluxo:

```text
Login
  ↓
Abrir caixa
  ↓
Adicionar produto
  ↓
Alterar quantidade
  ↓
Aplicar desconto
  ↓
Selecionar cliente
  ↓
Finalizar venda
  ↓
Pagamento
  ↓
Registrar venda
  ↓
Impressão
```

Não considere o deploy concluído apenas porque a página inicial abre.

---

# IMPRESSORA

O sistema possui integração com impressora de recibos.

Se houver código que utilize algo semelhante a:

```bash
printf "TESTE SEM SUDO\n\n\n" > /dev/usb/lp0
```

não altere essa lógica durante o deploy sem necessidade.

Lembre-se de que o sistema é **web**.

A arquitetura de impressão deve ser analisada cuidadosamente para determinar onde a impressora está fisicamente instalada.

Não faça alterações na lógica de impressão apenas para "fazer funcionar no servidor".

---

# SYSTEMD

Depois de criar o serviço:

```bash
systemctl daemon-reload
systemctl enable pdv
systemctl start pdv
```

Valide:

```bash
systemctl status pdv
```

E:

```bash
journalctl -u pdv -n 100 --no-pager
```

---

# NGINX

Após criar a configuração:

```bash
nginx -t
```

Se estiver OK:

```bash
systemctl reload nginx
```

Nunca reinicie o Nginx desnecessariamente.

---

# CLOUDFLARED

Depois da alteração:

```bash
systemctl status cloudflared
```

Verifique os logs e confirme que o tunnel continua saudável.

---

# ROLLBACK

Antes de modificar qualquer configuração importante, faça backup dos arquivos que serão alterados.

Principalmente:

```text
/etc/nginx/
/etc/cloudflared/
/etc/systemd/system/
```

Não altere arquivos de outros projetos sem backup.

Se alguma alteração quebrar um serviço existente, reverta imediatamente a alteração responsável.

---

# GIT

Antes do deploy, verifique:

```bash
git status
```

```bash
git branch
```

```bash
git log -5 --oneline
```

Não faça alterações destrutivas.

Não faça `git reset --hard` sem autorização explícita.

Não sobrescreva trabalho local existente.

---

# PRINCÍPIO DE EXECUÇÃO

Trabalhe seguindo este fluxo:

```text
1. AUDITAR
     ↓
2. PLANEJAR
     ↓
3. IDENTIFICAR RISCOS
     ↓
4. IMPLEMENTAR
     ↓
5. VALIDAR
     ↓
6. TESTAR
     ↓
7. DOCUMENTAR
```

Não pule diretamente para a implementação.

---

# AO FINAL

Apresente um relatório objetivo contendo:

## Infraestrutura

```text
Sistema operacional:
Python:
Django:
Banco:
Gunicorn:
Nginx:
Cloudflared:
```

## Portas

Informe:

```text
Gunicorn PDV:
Nginx:
Outros serviços encontrados:
```

## Arquivos criados/modificados

Liste todos.

Exemplo:

```text
/etc/systemd/system/pdv.service
/etc/nginx/sites-available/pdv
/etc/cloudflared/config.yml
.env
```

## Serviços

Informe o estado de:

```text
pdv.service
nginx
cloudflared
```

## URL

Confirme:

```text
https://pdv.wendrxw.online
```

## Testes realizados

Liste os testes executados e seus resultados.

## Problemas

Informe qualquer problema que permaneça.

**Não esconda warnings ou falhas.**

---

# REGRAS FINAIS

1. Não use `runserver` em produção.
2. Não execute Gunicorn como root.
3. Não exponha Gunicorn diretamente na Internet.
4. Não exponha portas desnecessariamente.
5. Não sobrescreva configurações de outros projetos.
6. Não remova serviços existentes.
7. Não altere DNS de outros subdomínios.
8. Não recrie o Cloudflare Tunnel sem necessidade.
9. Não coloque secrets no Git.
10. Não apague dados existentes.
11. Não faça migrations destrutivas.
12. Não use `git reset --hard` sem autorização.
13. Faça backup antes de alterar configurações críticas.
14. Sempre valide `nginx -t` antes de recarregar o Nginx.
15. Sempre valide o systemd depois de criar/modificar serviços.
16. Sempre teste o domínio externamente após o deploy.
17. Preserve o funcionamento dos demais sistemas hospedados no servidor.
18. Se encontrar uma decisão arquitetural que possa causar indisponibilidade, **pare e explique o problema antes de prosseguir**.

O resultado final esperado é um **PDV Django rodando de forma estável em produção**, acessível através de:

**https://pdv.wendrxw.online**
