# Correções generalistas (frontend e backend)

- Objetivo desse arquivo é anotar problemas encontrados no projeto. Vou anotar com descrições detalhadas, demonstrar prossiveis correções e até imagens, então será um documento bem rico;
- Não finalize os ajustes até que todos os problemas sejam corrigidos por inteiro;
- Baseado na main, crie uma branch chamada fix/03_09_2026;
- Não precisa criar PR, apenas faça o push na branch de trabalho, eu irei testar e realizar o merge posteriormente.

## Bugs, erros ou problemas encontrados

1. Na pagina de venda (https://pdv.wendrxw.online/app/pdv/venda/1ecdbd1b-7304-435f-bfd4-e5dc04b1b6a4/) a imagem do produto está cortada e em telas maiores o corte é ainda maior. Segue uma imagem para representar o problema: ![Captura de tela](/home/wendrew/Documentos/projetos/pdv/assets/ajuste1.png)
2. Corrija esse botão na pagina https://pdv.wendrxw.online/app/impressao/config/: 
                
```html
<a href="https://github.com/wendrxw/pdv/releases/latest/download/print-agent.exe" class="flex shrink-0 items-center gap-2 rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-bold text-white shadow-sm transition-colors hover:bg-brand-700">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-4 w-4"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="17 8 12 3 7 8"></polyline><line x1="12" x2="12" y1="3" y2="15"></line></svg>
</a>

```

Esse problema é grave, o botão fica de uma cor que não dá pra ve-lo na tela. Observe o problema, veja as cores dos outros botões, siga o padrão do site (UI/UX) e corrija esse problema;

3. Corrija esse botão na pagina https://pdv.wendrxw.online/app/impressao/estacoes/ 

```html

<a href="https://github.com/wendrxw/pdv/releases/latest/download/print-agent.exe" class="flex shrink-0 items-center gap-2 rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-bold text-white shadow-sm transition-colors hover:bg-brand-700">
                

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-4 w-4"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="17 8 12 3 7 8"></polyline><line x1="12" x2="12" y1="3" y2="15"></line></svg>


                Baixar agente (Windows)
            </a>
```
Esse problema é grave, o botão fica de uma cor que não dá pra ve-lo na tela. Observe o problema, veja as cores dos outros botões, siga o padrão do site (UI/UX) e corrija esse problema;

4. Quero que, você adicione uma opção no sistema, dentro de configurações, um botão para criar um atalho da tela de FRENTE DE CAIXA na area de trabalho (do computador/notebook do usuário). Quando o mesmo clicar, um atalho é criado na sua área de trabalho com redirecionamento para a tela de frente de caixa (com caixa aberto) e com fullscreen ativo, para parecer um sistema e não um site;
5. Na tela de cadastro de produtos, remova do formulário os asteriscos de TODOS OS CAMPOS, nenhum campo nessa tela é obrigatório (mantenha somente no nome do produto e no preço de venda);
6. Agora, vamos atuar num trecho muito importante do projeto: local-print-agent. Esse módulo é um dos mais importantes do projeto, pois integra o sistema com as impressoras locais do cliente. Basicamente, o software está no servidor e a impressora na maquina local do cliente. Mesmo instalando os drivers e firmwares das impressoras (etiqueta e fiscal/não fiscal) o sistema não reconhece os equipamentos, muito por conta dessa "distancia" de comunicação.

Parte do script principal do modulo é esse:
```python
# config.py

"""Configuração do Local Print Agent via variáveis de ambiente e
configuração local persistente.

Nada de segredo no código: URL do servidor, dispositivo da impressora e
credenciais vêm do ambiente ou da configuração local (estado do usuário).
A configuração local (state_dir/config.json) é criada pela configuração
interativa do primeiro uso e tem PRIORIDADE sobre os padrões do ambiente
— permite operação sem mexer em variáveis de sistema.
"""

import json
import os
from pathlib import Path


def _env(key, default=None):
    return os.environ.get(key, default)


def _env_int(key, default):
    try:
        return int(_env(key, str(default)))
    except (TypeError, ValueError):
        return default


class Config:
    """Parâmetros do agente (todos com padrões seguros)."""

    def __init__(
        self,
        server_url,
        device="/dev/usb/lp0",
        pair_code=None,
        largura_padrao="58",
        codepage="utf8",
        escpos=True,
        cortar_parcial=True,
        selecionar_codepage=True,
        alimentacao_final=8,
        label_device="",
        label_dpi=203,
        label_linguagem="epl2",
        poll_interval=3,
        http_timeout=30,
        state_dir=None,
        log_level="INFO",
        estacao_nome=None,
    ):
        self.server_url = server_url.rstrip("/")
        self.device = device
        self.pair_code = pair_code
        self.largura_padrao = largura_padrao
        self.codepage = codepage
        self.escpos = escpos
        self.cortar_parcial = cortar_parcial
        # Envia ESC t n no início (mesmo em modo texto) para o firmware
        # interpretar os acentos na tabela escolhida.
        self.selecionar_codepage = selecionar_codepage
        # Linhas em branco no fim do comprovante: o corte (guilhotina ou
        # rasgo manual) precisa de folga para não cair sobre o conteúdo.
        self.alimentacao_final = alimentacao_final
        # Impressora de ETIQUETAS (Elgin L42 Pro Full, EPL2). Vazio
        # desativa o suporte a etiquetas nesta estação.
        self.label_device = label_device
        self.label_dpi = label_dpi
        self.label_linguagem = label_linguagem
        self.poll_interval = poll_interval
        self.http_timeout = http_timeout
        self.state_dir = Path(state_dir or "~/.print-agent").expanduser()
        self.log_level = log_level
        self.estacao_nome = estacao_nome

    @classmethod
    def from_env(cls):
        """Lê as variáveis de ambiente (PRINT_AGENT_*)."""
        return cls(
            server_url=_env(
                "PRINT_AGENT_SERVER_URL", "https://pdv.wendrxw.online"
            ),
            device=_env("PRINTER_DEVICE", "/dev/usb/lp0"),
            pair_code=_env("PRINT_AGENT_PAIR_CODE") or None,
            largura_padrao=_env("PRINT_AGENT_LARGURA_PADRAO", "58"),
            codepage=_env("PRINTER_CODEPAGE", "utf8"),
            escpos=_env("PRINTER_ESCPOS", "1").lower()
            not in {"0", "false", "no", "off"},
            cortar_parcial=_env("PRINTER_CORTE_PARCIAL", "1").lower()
            not in {"0", "false", "no", "off"},
            selecionar_codepage=_env("PRINTER_SELECIONAR_CODEPAGE", "1").lower()
            not in {"0", "false", "no", "off"},
            alimentacao_final=_env_int("PRINTER_ALIMENTACAO_FINAL", 8),
            label_device=_env("PRINTER_LABEL_DEVICE") or "",
            label_dpi=_env_int("PRINTER_LABEL_DPI", 203),
            label_linguagem=_env("PRINTER_LABEL_LINGUAGEM", "epl2"),
            poll_interval=_env_int("PRINT_AGENT_POLL_INTERVAL", 3),
            http_timeout=_env_int("PRINT_AGENT_HTTP_TIMEOUT", 30),
            state_dir=_env("PRINT_AGENT_STATE_DIR"),
            log_level=_env("PRINT_AGENT_LOG_LEVEL", "INFO"),
            estacao_nome=_env("PRINT_AGENT_ESTACAO_NOME"),
        )

    @property
    def caminho_credencial(self):
        return self.state_dir / "credencial.json"

    @property
    def caminho_processados(self):
        return self.state_dir / "processados.jsonl"

    @property
    def caminho_config_local(self):
        return self.state_dir / "config.json"

    def aplicar(self, dados: dict):
        """Aplica valores da configuração local (somente os presentes)."""
        for chave in (
            "server_url",
            "device",
            "label_device",
            "largura_padrao",
            "codepage",
            "escpos",
            "cortar_parcial",
            "selecionar_codepage",
            "alimentacao_final",
            "label_dpi",
            "label_linguagem",
            "poll_interval",
            "http_timeout",
            "log_level",
            "estacao_nome",
        ):
            if chave in dados and dados[chave] not in (None, ""):
                setattr(self, chave, dados[chave])

    def salvar_local(self):
        """Persiste a configuração interativa (sem credenciais)."""
        self.state_dir.mkdir(parents=True, exist_ok=True)
        dados = {
            "server_url": self.server_url,
            "device": self.device,
            "label_device": self.label_device,
            "largura_padrao": self.largura_padrao,
            "codepage": self.codepage,
            "escpos": self.escpos,
            "cortar_parcial": self.cortar_parcial,
            "selecionar_codepage": self.selecionar_codepage,
            "alimentacao_final": self.alimentacao_final,
            "label_dpi": self.label_dpi,
            "label_linguagem": self.label_linguagem,
            "poll_interval": self.poll_interval,
            "http_timeout": self.http_timeout,
            "log_level": self.log_level,
            "estacao_nome": self.estacao_nome,
        }
        self.caminho_config_local.write_text(
            json.dumps(dados, ensure_ascii=False, indent=2), "utf-8"
        )

    @classmethod
    def carregar_local(cls, config):
        """Sobrepõe a configuração local salva (se existir)."""
        try:
            dados = json.loads(config.caminho_config_local.read_text("utf-8"))
        except (OSError, ValueError, json.JSONDecodeError):
            return config
        config.aplicar(dados)
        return config

```

Crie um plano de ação, planeja uma correção, faça testes unitários, testes de usabilidade e testes de integração. Quaisquer ação minha, pode me solicitar. Lembre-se, o usuário, em sua maioria usará Windows, então não daremos prioridade para integração com ambientes linux.

O executavel deve ser capaz de ser instalado no computador do usuário, reconhecer as impressoras presentes no sistema e deixar ambas funcional.

8. Como complemento da tarefa anterior, esse item terá como objetivo criar opções para usuário (em configurações) escolher qual a impressora fiscal e impressora para etiquetas, dessa forma, a impressão fica mais assertiva e o uso mais simples. Sendo assim, após a definição de uso das impressoras, a escolha deve ficar salva para todo o sistema, ou seja, na tela de impressão de etiquetas a escolha da impressora deve estar predefinido e o mesmo vale para a impressão de notas fiscai/não fiscais;

9. O objetivo desse item é coletar informações, documentar processos e atuar na tela de contato (https://pdv.wendrxw.online/contato/). Hoje, essa tela existe, está pronta (UI/UX), mas a funcionalidade do relatório não está de acordo, o submit funciona, mas não existe envio de informações. Como é um contato de fora (usuário - possivel cliente) para dentro (empresa - ONEMANCOMPANY) o fluxo pode ser mais simples. Vamos criar, no dashboard admin, uma pagina só para receber/ler esses pedidos de contato, precisa ser bem detalhado, completinho, para que o lead/funil de vendas funcione basicamente como o esperado;

10. Na tela de cadastro de produtos, o botão para adicionar um produto novo está invisivel aos olhos, pois segue a mesma cor de fundo da pagina:

```html

<a href="/app/produtos/novo/" class="flex items-center gap-2 rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-bold text-white shadow-sm transition-colors hover:bg-brand-700">
            

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-4 w-4"><path d="M5 12h14"></path><path d="M12 5v14"></path></svg>


            Novo produto
        </a>
```

Esse problema é grave, o botão fica de uma cor que não dá pra ve-lo na tela. Observe o problema, veja as cores dos outros botões, siga o padrão do site (UI/UX) e corrija esse problema.

## Criterios de aceite

- Testes unitarios, usabilidade, UI/UX;
- Prefira funcionalidade do que perfeição;
- Priorize segurança;
- Deixe o codigo sempre legivel e mantendo os padrões de desenvolvimento de codigo;
- Mantenabilidade é essencial;
- Legibilidade é importante;
- Use docstrings e comentarios em partes relevantes;
- Use tipagem;
- Sempre faça uso do que há demais recente no django;
- Antes de finalizar, execute uma varredura de vulnerabilidade, se encontrar algo, corrija;
- Realize commit e push na branch de trabalho e um pull na main antes de finalizar.